{
  "name": "mybazidestiny",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "pnpm seo:audit && node scripts/generate-og-images.mjs && astro build && node scripts/fileformat.mjs && node scripts/verify-dist.mjs",
    "seo:audit": "node scripts/seo-audit.mjs",
    "test:bazi": "node --experimental-strip-types tests/bazi-engine.test.ts",
    "test:payments": "node --experimental-strip-types tests/paypal-unlock.test.ts",
    "test": "pnpm test:bazi && pnpm test:payments",
    "preview": "astro preview",
    "postbuild": "echo done"
  },
  "dependencies": {
    "astro": "^5.12.0",
    "@astrojs/vercel": "^8.2.0",
    "@vercel/kv": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
